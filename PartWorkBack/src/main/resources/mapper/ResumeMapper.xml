<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.campus.partworkback.mapper.ResumeMapper">
    <insert id="addResumeUrl">
        INSERT INTO resume (user_id, file_path)
        VALUES (#{userId}, #{url})
            ON DUPLICATE KEY UPDATE
            file_path = VALUES(file_path),
            upload_time = CURRENT_TIMESTAMP
    </insert>

    <select id="getResumeUrl">
        select file_path from resume where user_id = #{userId}
    </select>
</mapper>